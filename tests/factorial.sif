(120)
@mul[int,int,int][int]
    (result, a, b)
    <0 <>3 <>1 .add
    (b, a, r+b)
    <>2 <>1 1 .sub
    <0

    @:mul
        <>1
        &mul .jmp
    ;
    @:mul
        .drop .drop
    ; .if .go
;

@fact[int][int]
    <0 1 .sub
    @:fact
        (x, x-1)
        <0 1 .sub .fact
        0 <>2 .mul
    ;
    @:fact
    ; .if .go
;

5 .fact .ipr "\n"